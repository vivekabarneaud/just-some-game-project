node_modules/
dist/
.idea/
*.local
.DS_Store
.env
.env.*
frontend/public/images/

# Original art: heavy source sketches, deliberately never tracked.
# Consolidated into art-sources/ on 2026-09-01 (was loose IMG_*.png in the repo
# root and in public/images/frames). See art-sources/README.md — git has never
# held these, so deleting them is permanent.
art-sources/

# Source map art (working files); processed web assets live in frontend/public/images/map/
/IMG_01*.png
